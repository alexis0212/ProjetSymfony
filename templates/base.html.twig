<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Gestion Boutique{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
    
    <style>
       .table-primary th {
        background: linear-gradient(180deg, #0078D7, #0056b3) !important; /* Couleur personnalisée avec dégradé */
        color: white !important; /* Texte blanc */
        text-align: center; /* Centrer le texte */
        font-weight: bold; /* Texte gras */
    }
    .btn-info {
        font-weight: bold; /* Met le texte en gras */
    }

    .table-primary {
        border: 1px solid #0056b3; /* Bordure bleue foncée */
    }
        body {
            display: flex;
            min-height: 100vh;
            font-family: Arial, sans-serif;
            margin: 0;
        }

        .sidebar {
            width: 250px;
            background: linear-gradient(180deg, #0056b3, #00aaff); /* Dégradé personnalisé */
            color: #fff; /* Texte blanc */
            display: flex;
            flex-direction: column;
            justify-content: flex-start; /* Gérer l'alignement vertical */
            padding: 20px;
            position: fixed;
            top: 0;
            left: 0;
            bottom: 0;
            font-size: 20px; /* Texte plus grand */
            font-weight: bold; /* Texte en gras */
        }

        .sidebar h2 {
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
            margin-bottom: 33px; /* Augmentation de l'espace sous le titre */
            font-weight: bold; /* Texte en gras */
            margin-top: 18px; /* Augmentation de l'espace au-dessus */
        }

        .sidebar h2 img {
            margin-right: 10px; /* Espace entre l'image et le texte */
            height: 95px; /* Taille ajustée de l'image */
        }

        .sidebar a {
            text-decoration: none;
            color: #fff;
            padding: 10px 15px;
            border-radius: 5px;
            margin-bottom: 15px; /* Réduction de l'espacement vertical */
            display: flex;
            align-items: center;
        }

        .sidebar a:hover {
            background: rgba(255, 255, 255, 0.2); /* Transparence blanche au survol */
        }

        .sidebar i {
            margin-right: 15px;
            font-size: 20px; /* Taille des icônes */
        }

        .content {
            margin-left: 260px; /* Espace pour la barre latérale */
            padding: 20px;
            flex: 1;
        }

        .sidebar .logout {
            margin-top: auto; /* Pousse le lien Déconnexion vers le bas */
        }
       .card {
    border: none; /* Supprime la bordure */
    border-radius: 10px;
    background: linear-gradient(180deg, #0056b3, #00aaff); /* Dégradé identique à la barre latérale */
    color: white; /* Texte blanc */
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Ombre douce */
}

.card p, .card strong {
    color: white; /* Texte blanc pour correspondre au design */
    font-weight: bold;
    font-size: 20px;
}

.bg-light {
    background-color: transparent !important; /* Pour supprimer tout conflit */
}
    </style>
</head>
<body>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    {% block sidebar %}
    <div class="sidebar">
        <h2>
            <img src="https://previews.123rf.com/images/valentint/valentint1602/valentint160202278/52100206-icono-de-la-tienda-en-l%C3%ADnea-bot%C3%B3n-de-internet-sobre-fondo-blanco.jpg" alt="Logo">
            GESTION BOUTIQUE
        </h2>
        <a href="#"><i class="fas fa-tachometer-alt"></i> Dashboard</a>
        <a href="{{ path('dette_index') }}"><i class="fas fa-file-invoice-dollar"></i> Dettes</a>
        <a href="{{ path('client_index') }}"><i class="fas fa-users"></i> Clients</a>
        <a href="{{ path('article_index') }}"><i class="fas fa-box"></i> Articles</a>
        <a href="{{ path('demande_index') }}"><i class="fas fa-envelope"></i> Demandes</a>
        <a href="{{ path('user_index') }}"><i class="fas fa-user"></i> Utilisateurs</a>
        <a href="#" class="logout"><i class="fas fa-sign-out-alt"></i> Déconnexion</a>
    </div>
    {% endblock %}
    <div class="content">
        {% block body %}{% endblock %}
    </div>
</body>
</html>
